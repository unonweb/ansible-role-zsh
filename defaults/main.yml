---
zsh:
  user:
  oh_my_zsh: true
  plugins:
    apt:
      - zsh-autosuggestions
      - zsh-syntax-highlighting
    files:
      - git.plugin.zsh
      - virtualenv.plugin.zsh
      - colored-man-pages.plugin.zsh
      - sudo.plugin.zsh
  themes:
    apt: []
    files: []
  config:
    src: