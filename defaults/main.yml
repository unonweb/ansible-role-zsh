---
zsh:
  user:
  oh_my_zsh:
  apt:
  - zsh-autosuggestions
  - zsh-syntax-highlighting
  - fzf
  plugins:
    files:
      - git.plugin.zsh
      - virtualenv.plugin.zsh
      - colored-man-pages.plugin.zsh
      - sudo.plugin.zsh
  themes:
    files: []
  config:
    src: