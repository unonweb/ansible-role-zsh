---
zsh:
  user:
  oh_my_zsh:
  apt:
    install:
    - zsh-autosuggestions
    - zsh-syntax-highlighting
    - fzf
    remove: []
  plugins:
    files: []    
  themes:
    files: []
  config:
    src:

# zsh.plugin.files:
# - git.plugin.zsh
# - virtualenv.plugin.zsh
# - colored-man-pages.plugin.zsh
# - sudo.plugin.zsh