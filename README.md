# TO DO

- Remove plugin & theme files for normal user & root
- Install plugin & theme files for root as well
- Automatically add `source "$HOME/.zsh/themes/<theme-file>` to zshrc
  for each theme file that is copied